[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cloud115"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "httpx",
    "pycryptodome",
    "lz4",
]

[tool.hatch.build.targets.wheel]
packages = ["src/cloud115"]
